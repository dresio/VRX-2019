#ifndef LISTEN_TO_GUI_H
#define LISTEN_TO_GUI_H

#include <the_planner/highLevelPlanner.h>
#include <the_planner/Missions.h>
//#include <usv16/PathPlannerAction.h>

class listen_to_gui : public Mission
{
public:
	//listen_to_gui(ros::NodeHandle &nh);
	//~listen_to_gui();
	//void mission_callback(const std_msgs::String::ConstPtr& msg);
	//void loop();
	//enum Task{START,LAST,FINISHED};

private:
	//ros::Publisher listen_to_gui_publisher;
	//ros::Subscriber listen_to_gui_subscriber;
	//void listen_to_gui_callback(const usv16::PathPlannerResult::ConstPtr& msg);
	//usv16::PathPlannerResult theTrajectory;
};

#endif
